# dependencies / build artifacts (local only)
/node_modules
/.next/
/out/
/build

# macOS
.DS_Store

# secrets / local env (KORDI_EMAIL lives here)
.env
.env*.local
*.pem

# pipe runtime output (local dedupe cache written at runtime)
/output/
